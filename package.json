{
  "name": "weddinginvite",
  "version": "1.0.0",
  "description": "Quick wedding invite",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "live-server --host=localhost --port=3000 --watch=./assets/css/site.css,./index.html",
    "start": "concurrently \"gulp watch\" \"npm run serve\"",
    "build": "gulp css"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mistersender/wedding-invite"
  },
  "author": "Jessica Kennedy",
  "license": "ISC",
  "browserslist": [
    "last 2 versions",
    "> 1%",
    "not dead"
  ],
  "dependencies": {
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^7.0.1",
    "gulp-sass": "^4.0.2"
  },
  "devDependencies": {
    "concurrently": "^5.0.0",
    "gulp-clean-css": "^4.2.0",
    "live-server": "^1.2.1"
  }
}
